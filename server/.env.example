# ===========================================
# CRM SFR BUSINESS - Configuration Production
# ===========================================
# Copiez ce fichier en .env et remplissez les valeurs

# ===========================================
# ENVIRONNEMENT
# ===========================================
NODE_ENV=production
PORT=3000

# ===========================================
# BASE DE DONNÉES
# ===========================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public&connection_limit=20
DATABASE_URL="postgresql://user:password@localhost:5432/crm_prise_rdv?schema=public&connection_limit=20"

# ===========================================
# AUTHENTIFICATION JWT
# ===========================================
# IMPORTANT: Générer un secret fort avec: openssl rand -base64 64
JWT_SECRET="CHANGEZ_MOI_EN_PRODUCTION_utilisez_openssl_rand_base64_64"

# Durée de validité du token (ex: 24h, 7d)
JWT_EXPIRES_IN=24h

# ===========================================
# CLIENT / CORS
# ===========================================
# URL de l'application frontend (pour CORS)
CLIENT_URL=https://crm.votre-domaine.com

# ===========================================
# EMAIL (SMTP) - Optionnel
# ===========================================
SMTP_HOST=smtp.exemple.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="CRM SFR Business <noreply@votredomaine.com>"

# ===========================================
# AIRCALL - Optionnel
# ===========================================
AIRCALL_API_ID=
AIRCALL_API_TOKEN=
AIRCALL_WEBHOOK_SECRET=

# ===========================================
# GOOGLE CALENDAR - Optionnel
# ===========================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

# ===========================================
# SÉCURITÉ ADDITIONNELLE
# ===========================================
# Activer Swagger en production (non recommandé)
ENABLE_SWAGGER=false

# ===========================================
# REDIS (pour rate limiting scalable) - Futur
# ===========================================
# REDIS_URL=redis://localhost:6379

# ===========================================
# LOGS
# ===========================================
# Niveau de log: error, warn, info, debug
LOG_LEVEL=info
